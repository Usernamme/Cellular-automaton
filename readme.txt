Run using life_run.py

The text field in the bottom right can be used to change the duration of
a single generation in milliseconds. The default is 500 ms.

Plans: add - an option for more states.
           - a working input button
